A few different ways to reverse a string in Javascript.

I was asked to make this during an interview recently.

reverse1 uses reverse() which is awesome, but doesn't demonstrate real problem solving capabilities.

reverse2 creates a new array, splits the 'word' to be reversed and then runs a decrementing for loop over the
length of the 'word', pushing each letter to our new array. Returns the array 